using System;
using System.Collections.Generic;
    class AverageLoadTimeCalculator
    {
        static void Main()
        {
            Console.WriteLine("!!!DOES NOT WORK!!!");
         /* Console.Write("How many sites will you enter: ");
           int height=int.Parse(Console.ReadLine()), width=3;
            string[] dateTime=null;
            string[] siteURL = null;
            string[] loadTime = null;
            int colcounter0 = 0;
            int colcounter1 = 0;
            int colcounter2 = 0;
            string[,] matrix = new string[height, width];
            for (int row = 0; row < height;row++)
            {
                for (int col = 0; col < width;col++)
                {
                    if (col == 0)
                    {
                        for (int i = 0; i <= colcounter0; i++)
                        {
                            Console.Write("Enter Date and Time: ");
                            dateTime[colcounter0] = Console.ReadLine();                          
                        }
                        colcounter0 += 1;
                    }
                    if (col == 1)
                    {
                        for (int i = 0; i <= colcounter0; i++)
                        {
                            Console.WriteLine("Enter Site URL: ");
                            siteURL[colcounter1] = Console.ReadLine();
                        }
                        colcounter1 += 1;
                    }
                    if (col == 2)
                    {
                        for (int i = 0; i <= colcounter0; i++)
                        {
                            Console.WriteLine("Enter Site load time: ");
                            loadTime[colcounter2] = Console.ReadLine();
                        }
                        colcounter2 += 1;
                    }
                }

            }
            for (int row = 0; row < height; row++)
            {
                for (int col = 0; col < width; col++)
                {
                    Console.Write(matrix[row, col] + " ");
                }
                Console.WriteLine();
            }*/
        }
    }
